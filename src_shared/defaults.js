const lat = 47.658779; // spokane latitude
const lng = -117.426048; // spokane longitude
const latLng = { lat: lat, lng: lng };
const gameMode = 'sectorControl';
const unknown = 'unknown!';
const deviceType = 'virt2b';

module.exports = {
  lat,
  lng,
  latLng,
  gameMode,
  unknown,
  deviceType
}
